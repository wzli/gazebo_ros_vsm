---
version: "2.3"

services:
  sim_node:
    scale: 6
    image: gazebo_ros_vsm_demo
    ports:
      - 8080
    environment:
      - SPAWN_YAW=-1.57
      - VSM_RANGE=10
      - VSM_EXPIRY=10000
      - VSM_VERBOSITY=2
    build:
      dockerfile: docker/Dockerfile
      context: ..
